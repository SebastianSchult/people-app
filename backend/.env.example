# === MariaDB Verbindung (Beispiel) ===
# Wähle EINE Variante (Socket ODER TCP) und passe sie an.

# --- Variante A: Unix-Socket (empfohlen lokal) ---
# DB_SOCKET=/tmp/mysql.sock

# --- Variante B: TCP ---
# DB_HOST=127.0.0.1
# DB_PORT=3306

DB_USER=app
DB_PASSWORD=app
DB_NAME=people